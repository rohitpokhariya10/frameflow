PORT=3001
GEMINI_API_KEY=
GEMINI_IMAGE_MODEL=gemini-3.1-flash-image
AI_TIMEOUT_MS=120000
# Set to the exact Vercel frontend origin in production.
CLIENT_ORIGIN=http://localhost:5173
# Render has one trusted proxy hop. Keep 0 for direct local access; set 1 on Render.
TRUST_PROXY_HOPS=0
